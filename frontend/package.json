{
  "name": "wine-demo-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --mode ops",
    "dev:ops": "vite --mode ops",
    "dev:pulse": "vite --mode pulse",
    "build": "npm run build:ops && npm run build:pulse",
    "build:ops": "vite build --mode ops",
    "build:pulse": "vite build --mode pulse"
  },
  "dependencies": {
    "@lucide/vue": "^1.28.0",
    "vue": "^3.4.38"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.2.1"
  }
}
